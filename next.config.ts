import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["play-lh.googleusercontent.com" , "static.thenounproject.com" , "picsum.photos"],
  },
};

export default nextConfig;
